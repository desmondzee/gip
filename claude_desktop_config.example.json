{
  "_comment": "Persona MCP server. Copy into ~/Library/Application Support/Claude/claude_desktop_config.json (merge with other entries if present), fill in absolute paths and env vars, then restart Claude Desktop. Both the bun binary path AND the bin/mcp-persona.ts path MUST be absolute — Claude Desktop spawns the MCP with a sandboxed environment, no shell, no .env autoload, no inherited PATH.",
  "mcpServers": {
    "persona": {
      "command": "/opt/homebrew/bin/bun",
      "args": [
        "run",
        "/ABSOLUTE/PATH/TO/amman/bin/mcp-persona.ts"
      ],
      "env": {
        "ANTHROPIC_API_KEY": "sk-ant-...",
        "ANTHROPIC_MODEL": "claude-sonnet-4-6",
        "MONGODB_URI": "mongodb+srv://USER:PASS@CLUSTER/?retryWrites=true&w=majority",
        "MONGODB_DB": "persona",
        "PERSONA_USER_NAME": "Jerry",
        "GEMINI_API_KEY": "..."
      }
    }
  }
}
